# -*- mode: snippet -*-
# name: hook_menu_default_items_alter
# key: hook_menu_default_items_alter
# --
/**
 * ${2:Alter the default menu items right before they are cached into the database.
 *}
 * @param &$items
 *   By reference. The menu items that have been declared by another feature.
 */
function ${1:`(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`}_menu_default_items_alter(&$items) {
$0}
