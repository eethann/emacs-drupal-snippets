# -*- mode: snippet -*-
# name: hook_field_attach_delete
# key: hook_field_attach_delete
# --
/**
 * ${2:Act on field_attach_delete().
 *
 * This hook is invoked after the field module has performed the operation.
 *
 * See field_attach_delete() for details and arguments.}
 */
function ${1:`(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`}_field_attach_delete($entity_type, $entity) {
$0  // @todo Needs function body.
}
