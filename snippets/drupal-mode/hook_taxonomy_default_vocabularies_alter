# -*- mode: snippet -*-
# name: hook_taxonomy_default_vocabularies_alter
# key: hook_taxonomy_default_vocabularies_alter
# --
/**
 * ${2:Alter the default vocabularies right before they are cached into the
 * database.
 *}
 * @param &$vocabularies
 *   By reference. The vocabularies that have been declared by another feature.
 */
function ${1:`(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`}_taxonomy_default_vocabularies_alter(&$vocabularies) {
$0}
