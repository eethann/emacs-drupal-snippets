# -*- mode: snippet -*-
# name: hook_rules_evaluator_info_alter
# key: hook_rules_evaluator_info_alter
# --
/**
 * ${2:Alter rules input evaluator info.
 *
 * The implementation should be placed into the file MODULENAME.rules.inc, which
 * gets automatically included when the hook is invoked.
 *}
 * @param $evaluator_info
 *   The items of all modules as returned from hook_rules_evaluator_info().
 *
 * @see hook_rules_evaluator_info()
 */
function ${1:`(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`}_rules_evaluator_info_alter(&$evaluator_info) {
$0  // Change evaluator info.
}
