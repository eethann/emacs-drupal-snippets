# -*- mode: snippet -*-
# name: hook_ckeditor_link_TYPE_url
# key: hook_ckeditor_link_TYPE_url
# --
/**
 * ${2:Convert an internal path into an aliased and, if applicable, language
 * prefixed URL.
 *}
 * @param $path
 *   The internal path to convert.
 * @param $langcode
 *   The language code of the path if any, the language code of the text to be
 *   filtered otherwise. It should only be used as a fallback when the content
 *   being linked to does not have any intrisic language.
 *
 * @return
 *   An URL alias, or nothing if the implementing module is not responsible for
 *   the given path.
 */
function ${1:`(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`}_ckeditor_link_TYPE_url($path, $langcode) {
$0  //
}
