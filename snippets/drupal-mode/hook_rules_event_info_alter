# -*- mode: snippet -*-
# name: hook_rules_event_info_alter
# key: hook_rules_event_info_alter
# --
/**
 * ${2:Alter rules events.
 *
 * The implementation should be placed into the file MODULENAME.rules.inc, which
 * gets automatically included when the hook is invoked.
 *}
 * @param $events
 *   The items of all modules as returned from hook_rules_event_info().
 *
 * @see hook_rules_event_info().
 */
function ${1:`(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`}_rules_event_info_alter(&$events) {
$0  // Change events.
}
