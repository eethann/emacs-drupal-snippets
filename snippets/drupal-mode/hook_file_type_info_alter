# -*- mode: snippet -*-
# name: hook_file_type_info_alter
# key: hook_file_type_info_alter
# --
/**
 * ${2:Perform alterations on file types.
 *}
 * @param $info
 *   Array of information on file types exposed by hook_file_type_info()
 *   implementations.
 */
function ${1:`(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`}_file_type_info_alter(&$info) {
$0  // @todo Add example.
}
