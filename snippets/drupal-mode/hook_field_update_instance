# -*- mode: snippet -*-
# name: hook_field_update_instance
# key: hook_field_update_instance
# --
/**
 * ${2:Act on a field instance being updated.
 *
 * This hook is invoked from field_update_instance() after the instance record
 * is saved, so it cannot be used by a module to modify the instance itself.
 *}
 * @param $instance
 *   The instance as it is post-update.
 * @param $prior_$instance
 *   The instance as it was pre-update.
 */
function ${1:`(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`}_field_update_instance($instance, $prior_instance) {
$0  // @todo Needs function body.
}
