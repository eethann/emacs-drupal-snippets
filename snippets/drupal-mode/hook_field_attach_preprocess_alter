# -*- mode: snippet -*-
# name: hook_field_attach_preprocess_alter
# key: hook_field_attach_preprocess_alter
# --
/**
 * ${2:Alter field_attach_preprocess() variables.
 *
 * This hook is invoked while preprocessing the field.tpl.php template file
 * in field_attach_preprocess().
 *}
 * @param $variables
 *   The variables array is passed by reference and will be populated with field
 *   values.
 * @param $context
 *   An associative array containing:
 *   - entity_type: The type of $entity; for example, 'node' or 'user'.
 *   - entity: The entity with fields to render.
 *   - element: The structured array containing the values ready for rendering.
 */
function ${1:`(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`}_field_attach_preprocess_alter(&$variables, $context) {
$0  // @todo Needs function body.
}
